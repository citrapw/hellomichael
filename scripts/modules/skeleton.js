define(function (require, exports, module) {
    'use strict';

    var skeleton = (function () {
        // Public Functions
        var init = function () {
            bindUI();
        };

        var destroy = function () {

        };

        // Private Functions
        var bindUI = function () {
         
        };

        return {
            init: init
        };
    })();

    return skeleton;
});